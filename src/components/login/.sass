@import ../common
@import ../bg-moving-triangle/.sass

=m-triangle-menu($margin-y: $md)
	.m-triangle-menu
		.button
			transition: transform $fast, opacity $slow
			+margin($y: $margin-y)
			+bg-moving-triangle($margin-y, $trigger: '&.hovering, &.active')
			
			+alternate-bg-text($trigger: '&:hover, &.active')
			font-size: $font-md

			.background
				+border($color: currentColor)

			.color
				user-select: none
				transition: transform $fast
				line-height: 1em

		.login
			color: $green0
		.register
			color: $red0

		.button.active
			
			.fancy-square-container
				opacity: 1
				.border-square
					transform: translate(0, -50%) rotate(45deg)

			.color 
				+scale(0.75, $origin: center)
			&.hovering
				.fancy-square-container
					+scale(1.1, $origin: center)


		&.active .button:not(.active)
			+translate($y: 100vh)
			opacity: 0

.m-login
	max-width: $max-reading-width / 2
	margin: auto
	position: relative
	+m-triangle-menu()

.back-button
	position: absolute
	left: 5px
	color: white
	opacity: 0
	transition: opacity $fast