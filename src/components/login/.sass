@import ../common

.registration.form-items.menu
	position: relative
	margin: auto
	max-width: $max-reading-width / 2

	&.g_active .instance:not(.l_active)
		+translate($y: 100vh)
		+hide

	.form-item-instance
		$margin: 2.5em
		+margin($y: $margin)
		.the-toggle .form-name
			+padding($y: 10px)

		=push-triangles-out
			.tri.align-top
				+t-up
			.tri.align-bottom
				+t-down

		&.hovering, &.l_active
			+push-triangles-out
			.the-toggle
				+cc-swap
				.form-name .matrix
					+scale(0.75, $origin: center)
			svg.tri
				+show

		
		&.l_active
			.svg-button-0 

				+show
				.rotation
					transform: rotate(45deg)

		svg.tri
			+hide
			height: $margin
			width: 20%
			left: 40%
		.svg-button-0
			+hide

		=colors
			&.login, &.login svg
				color: $green0
			&.register, &.register svg
				color: $red0
		+colors
.over-hidden
	overflow: hidden