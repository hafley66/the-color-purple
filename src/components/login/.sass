@import ../common
@import ../bg-moving-triangle/.sass

=m-triangle-menu($margin-y: $md)
	.m-triangle-menu
		.button
			transition: transform $fast, opacity $slow
			+margin($y: $margin-y)
			+bg-moving-triangle($margin-y, $trigger: '&:hover, &.active')
			
			+border($color: currentColor)
			+alternate-bg-text($trigger: '&:hover, &.active')
			font-size: $font-md

			.color
				user-select: none
				transition: transform $fast
				line-height: 1em

		.login
			color: $green0
		.register
			color: $red0

		.button.active
			.back-button
				opacity: 1
			.color 
				+scale(0.75, $origin: center)
			&:hover .color
				+scale(1)

		&.active .button:not(.active)
			+translate($y: 100vh)
			opacity: 0

.m-login
	max-width: $max-reading-width / 2
	margin: auto
	position: relative
	+m-triangle-menu()

.back-button
	position: absolute
	left: 5px
	color: white
	opacity: 0
	transition: opacity $fast